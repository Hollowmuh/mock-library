{
  "name": "library-app",
  "version": "1.0.0",
  "description": "A simple library application to manage and display a collection of books",
  "main": "src/scripts/script.js",
  "scripts": {
    "test": "jest",
    "start": "serve src"
  },
  "dependencies": {
    "crypto": "^1.0.1",
    "serve": "^14.0.0"
  },
  "devDependencies": {
    "@babel/core": "^7.18.0",
    "@babel/preset-env": "^7.18.0",
    "babel-jest": "^28.0.0",
    "jest": "^28.0.0"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },
  "jest": {
    "transform": {
      "^.+\\.js$": "babel-jest"
    }
  }
}
